# 发布到 VSCode Marketplace 和 Open VSX 指南

## 📦 当前状态
- ✅ 已打包：`sy-ai-commit-1.0.7.vsix`
- ⚠️ 商城版本：1.0.5（需要更新）
- 🎯 目标版本：1.0.7

---

## 🚀 方式一：使用命令行发布到 VSCode Marketplace

### 前置准备

#### 1. 获取 Personal Access Token (PAT)

**步骤：**
1. 访问：https://dev.azure.com/
2. 登录你的 Microsoft 账号
3. 点击右上角头像 → **Personal access tokens**
4. 点击 **New Token**
5. 配置 Token：
   - **Name**: `vsce-publish`（随意命名）
   - **Organization**: 选择 **All accessible organizations**
   - **Expiration**: 建议选择 **Custom defined**，设置较长时间（如 90 天或 1 年）
   - **Scopes**: 选择 **Custom defined**
     - 展开 **Marketplace**
     - 勾选 **Acquire** 和 **Manage**
6. 点击 **Create**
7. **重要**：复制生成的 Token（只显示一次，请保存好）

#### 2. 登录 vsce

```bash
# 使用 Token 登录（publisher 名称在 package.json 中）
vsce login sy-ai-commit

# 粘贴你的 Personal Access Token
# 输入后回车
```

### 发布步骤

#### 方式 A：直接发布（推荐）
```bash
# 自动编译、打包并发布
npm run publish
```

或者：
```bash
vsce publish
```

#### 方式 B：发布已有的 VSIX 文件
```bash
vsce publish --packagePath sy-ai-commit-1.0.7.vsix
```

### 发布成功提示
```
Publishing sy-ai-commit@1.0.7...
Successfully published sy-ai-commit@1.0.7!
Your extension will live at https://marketplace.visualstudio.com/items?itemName=sy-ai-commit.sy-ai-commit (might take a few minutes for it to show up).
```

---

## 🌐 方式二：手动上传到 VSCode Marketplace

### 步骤：

1. **访问发布者页面**
   - 网址：https://marketplace.visualstudio.com/manage/publishers/sy-ai-commit
   - 用 Microsoft 账号登录

2. **找到你的扩展**
   - 在扩展列表中找到 `sy-ai-commit`
   - 点击扩展名称进入详情

3. **上传新版本**
   - 点击右上角 **Update** 或 **...** → **Update**
   - 选择文件：`sy-ai-commit-1.0.7.vsix`
   - 点击 **Upload**

4. **等待审核**
   - 通常几分钟到几小时
   - 审核通过后自动发布

---

## 📘 方式三：发布到 Open VSX Registry（Cursor 可能使用）

### 前置准备

#### 1. 注册 Open VSX 账号
- 访问：https://open-vsx.org/
- 使用 GitHub 账号登录

#### 2. 创建 Access Token
1. 登录后访问：https://open-vsx.org/user-settings/tokens
2. 点击 **Create Token**
3. 复制生成的 Token

#### 3. 安装 ovsx 工具
```bash
npm install -g ovsx
```

### 发布步骤

```bash
# 方式 A：直接发布当前目录
ovsx publish -p YOUR_ACCESS_TOKEN

# 方式 B：发布指定的 VSIX 文件
ovsx publish sy-ai-commit-1.0.7.vsix -p YOUR_ACCESS_TOKEN
```

### 保存 Token（可选）
```bash
# 设置环境变量，避免每次输入
# Windows PowerShell:
$env:OVSX_PAT="YOUR_ACCESS_TOKEN"

# Linux/macOS:
export OVSX_PAT="YOUR_ACCESS_TOKEN"

# 之后直接运行
ovsx publish
```

---

## 🔄 同时发布到两个市场

### 方法 1：添加 npm 脚本

在 `package.json` 的 `scripts` 中添加：

```json
{
  "scripts": {
    "publish:vscode": "vsce publish",
    "publish:ovsx": "ovsx publish -p $OVSX_PAT",
    "publish:all": "npm run publish:vscode && npm run publish:ovsx"
  }
}
```

使用：
```bash
npm run publish:all
```

### 方法 2：手动依次执行

```bash
# 1. 发布到 VSCode Marketplace
vsce publish

# 2. 发布到 Open VSX
ovsx publish -p YOUR_OVSX_TOKEN
```

---

## ✅ 发布后的检查清单

### VSCode Marketplace
- [ ] 访问：https://marketplace.visualstudio.com/items?itemName=sy-ai-commit.sy-ai-commit
- [ ] 检查版本号是否显示为 1.0.7
- [ ] 检查 README 显示是否正常
- [ ] 检查截图/GIF 是否正常显示
- [ ] 尝试搜索插件名称，能否找到
- [ ] 在 VSCode/Cursor 中搜索安装测试

### Open VSX
- [ ] 访问：https://open-vsx.org/extension/sy-ai-commit/sy-ai-commit
- [ ] 检查版本号是否显示为 1.0.7
- [ ] 检查信息是否完整

---

## 📊 版本发布策略

### 小版本更新（1.0.x）
- Bug 修复
- 文档更新
- 小的功能优化

**发布命令：**
```bash
# 自动升级 patch 版本并发布
vsce publish patch
```

### 中版本更新（1.x.0）
- 新功能添加
- 较大的改进

**发布命令：**
```bash
# 自动升级 minor 版本并发布
vsce publish minor
```

### 大版本更新（x.0.0）
- 重大更新
- 破坏性变更

**发布命令：**
```bash
# 自动升级 major 版本并发布
vsce publish major
```

---

## 🐛 常见问题

### Q1: 发布时提示 "Permission denied"
**原因**：Token 权限不足或已过期

**解决方案：**
1. 重新生成 Personal Access Token
2. 确保勾选了 Marketplace 的 Acquire 和 Manage 权限
3. 重新登录：`vsce login sy-ai-commit`

### Q2: 发布后商城不显示新版本
**原因**：需要等待缓存更新

**解决方案：**
- 等待 5-30 分钟
- 清除浏览器缓存
- 在 VSCode 中强制刷新扩展列表

### Q3: 提示 "This extension is already published"
**原因**：版本号已存在

**解决方案：**
1. 检查 `package.json` 中的版本号
2. 确保版本号大于已发布的版本
3. 或使用 `vsce publish patch/minor/major` 自动升级版本

### Q4: Open VSX 提示 "Namespace not found"
**原因**：首次发布需要先创建 namespace

**解决方案：**
1. 访问 https://open-vsx.org/user-settings/namespaces
2. 创建 namespace：`sy-ai-commit`
3. 重新发布

---

## 📝 发布前的最终检查

- [ ] 运行测试：`npm run test`（如果有）
- [ ] 检查 Linter：`npm run lint`
- [ ] 更新 CHANGELOG.md
- [ ] 更新 package.json 版本号
- [ ] 更新 README.md（如有必要）
- [ ] 本地测试插件功能
- [ ] 提交所有代码到 Git
- [ ] 创建 Git tag：`git tag v1.0.7 && git push origin v1.0.7`

---

## 🎯 快速发布流程（推荐）

```bash
# 1. 确保代码已提交
git add .
git commit -m "chore: 发布 v1.0.7"
git push

# 2. 创建 Git 标签
git tag v1.0.7
git push origin v1.0.7

# 3. 发布到 VSCode Marketplace
vsce publish

# 4. 发布到 Open VSX（可选）
ovsx publish -p $OVSX_PAT

# 完成！🎉
```

---

## 📞 获取帮助

- VSCode Marketplace 文档：https://code.visualstudio.com/api/working-with-extensions/publishing-extension
- Open VSX 文档：https://github.com/eclipse/openvsx/wiki/Publishing-Extensions
- vsce GitHub：https://github.com/microsoft/vscode-vsce
- ovsx GitHub：https://github.com/eclipse/openvsx

---

**祝发布顺利！🚀**

