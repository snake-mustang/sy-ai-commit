# 📦 VSCode 插件发布指南

## 准备工作

### 1. 安装依赖
```bash
npm install
```

### 2. 编译项目
```bash
npm run compile
```

### 3. 本地测试
按 `F5` 启动调试模式，会打开一个新的 VSCode 窗口，在新窗口中测试插件功能。

## 发布到 VSCode 市场

### 步骤 1：注册 Azure DevOps 账号

1. 访问 [Azure DevOps](https://dev.azure.com/)
2. 使用 Microsoft 账号登录
3. 创建个人访问令牌（Personal Access Token, PAT）：
   - 点击右上角用户头像 → "Security"
   - 选择 "Personal access tokens" → "New Token"
   - 名称：VSCode Publisher
   - Organization：All accessible organizations
   - Scopes：选择 "**Marketplace** → **Manage**"
   - 过期时间：建议选择 90 天或自定义
   - 点击创建，**务必保存生成的 Token**

### 步骤 2：创建 Publisher

1. 访问 [Visual Studio Marketplace Publisher Management](https://marketplace.visualstudio.com/manage)
2. 点击 "Create publisher"
3. 填写信息：
   - ID：your-publisher-name（这个要在 package.json 中使用）
   - Name：显示名称
   - Email：邮箱地址
4. 创建成功后，将 package.json 中的 `publisher` 字段改为您的 Publisher ID

### 步骤 3：安装 vsce 工具

```bash
npm install -g @vscode/vsce
```

### 步骤 4：登录 Publisher

```bash
vsce login your-publisher-name
```

输入您在步骤 1 中创建的 Personal Access Token。

### 步骤 5：准备图标（可选但推荐）

在项目根目录放置一个 `icon.png` 文件，尺寸建议 128x128 或 256x256 像素。

### 步骤 6：打包插件

```bash
npm run package
```

这会生成一个 `.vsix` 文件，可用于手动安装测试。

### 步骤 7：发布到市场

```bash
npm run publish
```

或者使用 vsce 命令：
```bash
vsce publish
```

如果是首次发布，使用：
```bash
vsce publish 1.0.0
```

## 更新版本

### 1. 更新版本号

修改 `package.json` 中的 `version` 字段，遵循语义化版本：
- 主版本号：重大更新
- 次版本号：新功能
- 修订号：Bug 修复

示例：`1.0.0` → `1.1.0`（新功能）或 `1.0.1`（Bug 修复）

### 2. 更新 CHANGELOG.md

记录本次更新的内容。

### 3. 发布新版本

```bash
# 自动递增修订号并发布（1.0.0 → 1.0.1）
vsce publish patch

# 自动递增次版本号并发布（1.0.0 → 1.1.0）
vsce publish minor

# 自动递增主版本号并发布（1.0.0 → 2.0.0）
vsce publish major
```

## 常见问题

### Q1: 发布失败，提示 "Missing publisher name"
**A:** 请在 `package.json` 中添加 `publisher` 字段。

### Q2: 发布失败，提示 "Missing README.md"
**A:** 确保项目根目录有 `README.md` 文件。

### Q3: 发布失败，提示 "Missing LICENSE"
**A:** 确保项目根目录有 `LICENSE` 文件。

### Q4: 如何撤销发布的版本？
**A:** 使用以下命令：
```bash
vsce unpublish your-publisher-name.auto-commit-assistant@1.0.0
```

### Q5: 如何查看插件统计数据？
**A:** 访问 [Marketplace Publisher Management](https://marketplace.visualstudio.com/manage/publishers/your-publisher-name)

## 最佳实践

1. ✅ **本地充分测试**：确保所有功能正常工作
2. ✅ **版本号规范**：遵循语义化版本规范
3. ✅ **更新文档**：每次发布前更新 CHANGELOG.md
4. ✅ **使用 Git 标签**：为每个发布版本打标签
5. ✅ **图标设计**：提供清晰的图标以提升辨识度
6. ✅ **README 完善**：提供详细的使用说明和示例

## 快速命令参考

```bash
# 安装依赖
npm install

# 编译
npm run compile

# 监听文件变化并自动编译
npm run watch

# 代码检查
npm run lint

# 打包
npm run package

# 发布
npm run publish

# 发布补丁版本
vsce publish patch

# 发布次版本
vsce publish minor

# 发布主版本
vsce publish major
```

## 相关链接

- [VSCode 扩展 API 文档](https://code.visualstudio.com/api)
- [VSCode 扩展发布指南](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)
- [Visual Studio Marketplace](https://marketplace.visualstudio.com/)
- [Azure DevOps](https://dev.azure.com/)

---

祝您发布顺利！🎉

