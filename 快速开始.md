# 🚀 快速开始

## 📋 前提条件

- Node.js 18.x 或更高版本
- npm 或 yarn
- VSCode 或 Cursor 编辑器

## 🎯 5 分钟上手

### 1️⃣ 安装依赖

```bash
npm install
```

### 2️⃣ 编译项目

```bash
npm run compile
```

### 3️⃣ 启动调试

在 VSCode/Cursor 中：
1. 按 `F5` 键
2. 选择"运行扩展"
3. 等待新窗口打开

### 4️⃣ 测试功能

在新打开的窗口中：

#### 方法 1：使用命令面板
1. 按 `Ctrl+Shift+P`（Mac: `Cmd+Shift+P`）
2. 输入 `AI Auto Commit`
3. 选择"初始化配置"
4. 等待配置完成

#### 方法 2：自动提示
1. 打开任意工作区
2. 等待自动提示弹出
3. 点击"是"进行配置

### 5️⃣ 使用快捷键

配置完成后：
1. 修改项目中的任意文件
2. 按 `Ctrl+G Ctrl+G`
3. 观察自动执行的流程：
   - ✅ 保存文件
   - ✅ 暂存更改
   - ✅ 生成提交信息
   - ✅ 执行提交

## 📂 项目结构说明

```
├── src/extension.ts       ← 核心代码在这里
├── package.json          ← 插件配置和元数据
├── tsconfig.json         ← TypeScript 配置
└── README.md            ← 用户文档
```

## 🔧 开发命令

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run compile` | 编译 TypeScript |
| `npm run watch` | 监听文件变化自动编译 |
| `npm run lint` | 代码检查 |
| `npm run package` | 打包为 .vsix |
| `npm run publish` | 发布到市场 |

## 🐛 调试技巧

### 查看日志
1. 在扩展开发宿主窗口打开"输出"面板
2. 选择"扩展主机"
3. 查看控制台输出

### 重新加载
在扩展开发宿主窗口按 `Ctrl+R` 重新加载扩展

### 断点调试
1. 在代码中设置断点
2. 按 `F5` 启动调试
3. 触发相应功能

## ⚙️ 自定义配置

在 VSCode/Cursor 设置中搜索 `Auto Commit Assistant`：

```json
{
  // 首次打开时自动配置
  "autoCommitAssistant.autoSetupOnStartup": true,
  
  // 自定义快捷键
  "autoCommitAssistant.keybinding": "ctrl+g ctrl+g",
  
  // 提交后自动推送
  "autoCommitAssistant.enablePush": false
}
```

## 📝 修改功能

### 更改快捷键
编辑 `package.json` 中的默认值：
```json
"autoCommitAssistant.keybinding": {
  "default": "ctrl+alt+c"  // 改为您想要的快捷键
}
```

### 修改 Cursor 规则
编辑 `src/extension.ts` 中的 `gitCommitRules` 变量

### 添加新命令
1. 在 `package.json` 的 `commands` 中注册
2. 在 `src/extension.ts` 中实现逻辑

## 🚀 发布准备

### 1. 修改发布者信息

编辑 `package.json`：
```json
{
  "publisher": "your-publisher-name",  // 改为您的 Publisher ID
  "repository": {
    "url": "https://github.com/your-username/auto-commit-assistant.git"
  }
}
```

### 2. 准备图标

在根目录放置 `icon.png`（128x128 或 256x256 像素）

### 3. 创建 Publisher

访问 [Visual Studio Marketplace](https://marketplace.visualstudio.com/manage) 创建

### 4. 安装 vsce

```bash
npm install -g @vscode/vsce
```

### 5. 登录并发布

```bash
vsce login your-publisher-name
vsce publish
```

## 📚 更多资源

- 📖 [开发指南.md](./开发指南.md) - 详细开发文档
- 📦 [发布指南.md](./发布指南.md) - 完整发布流程
- 📝 [README.md](./README.md) - 用户使用文档

## ❓ 常见问题

**Q: 编译失败？**
```bash
# 清理并重新安装
rm -rf node_modules
npm install
npm run compile
```

**Q: 调试窗口无法启动？**
- 确保已编译项目（`npm run compile`）
- 检查是否有 TypeScript 错误

**Q: 快捷键不生效？**
- 检查 keybindings.json 是否正确更新
- 重启 VSCode/Cursor

**Q: 找不到命令？**
- 确认 package.json 中已注册命令
- 确认已在 extension.ts 中实现

## 💡 开发建议

1. ✅ 使用 `npm run watch` 自动编译
2. ✅ 经常按 `Ctrl+R` 重新加载测试
3. ✅ 使用 `console.log` 调试
4. ✅ 查看"输出"面板的错误信息
5. ✅ 提交前运行 `npm run lint`

---

**祝您开发顺利！有问题随时查看详细文档。** 🎉

